-r requirements.txt

# Server/container dependencies. Desktop-only pywebview and pystray are
# intentionally omitted because the container serves the UI over HTTP.
yt-dlp==2026.7.4
faster-whisper==1.2.1
openai==2.49.0
google-genai==2.14.0
opencv-python-headless==4.13.0.92
# The generic (amd64) container uses the current security-fixed release.
Pillow==12.3.0
