apiVersion: v1
kind: Service
metadata:
  name: {{ include "shorts-studio.fullname" . }}
  labels:
    {{- include "shorts-studio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "shorts-studio.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
